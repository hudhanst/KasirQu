import React, { Fragment } from 'react'

import { connect } from 'react-redux'

class UserDataList extends React.Component {
    render() {
        return (
            <Fragment>
            </Fragment>
        )
    }
}

export default connect(null, {})(UserDataList)